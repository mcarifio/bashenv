source /etc/bashrc
# u.map.tree source $(bashenv.root)/.bashrc.d
# u.map.tree source $(bashenv.root)/.bash_completion.d
# u.map.tree guard $(bashenv.root)/.bashrc.d

# run all bashenv functions of the form ${something}.session.
session.start
