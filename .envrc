# direnv allow .
export TOP=$(realpath -Lm $(dirname ${BASH_SOURCE%*/}))
PATH_add $(find ${TOP} -type d -name bin)

for _c in envsubst just; do type -P ${_c} > /dev/null || >&2 echo "command ${_c} not on PATH?"; done
